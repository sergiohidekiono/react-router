import Pagina from "../../components/template/Pagina";

export default function (props) {
    return <Pagina titulo="Contato" menu={props.menu}  links={props.links} descricao="Fale com a gente"></Pagina>;
}
